#include<stdio.h>
static int alpha=12;
/*void main()
{
	int limit,nums;
	scanf("%d",&limit);
	for(nums=2;nums<=limit;nums++)
	{
		if(nums==2||nums==3||nums==5||nums==7
		||nums%2!=0&&nums%3!=0&&nums%5!=0&&nums%7!=0)
		{
			printf("\n%d",nums);
		}
	}
}

int n,initial,final,count;
	printf("enter the number");
	scanf("%d",&n);
	printf("enter the prime numbers:");
	for(initial=1;initial<=n;initial++){
		count=0;
		for(final=1;final<=n;final++){
			
			if(initial%final==0)
			count++;
		}
		if(count==2)
		printf("\n%d",initial);
	}
*/
